package  {		import flash.display.MovieClip;	import com.caurina.transitions.Tweener;	import flash.events.*;			public class bug extends MovieClip {				public var DC;				public function bug(DC) {			this.DC = DC;			this.x = randomRange(0, 1200);			this.y = randomRange(0, 1000);			this.addEventListener(Event.ENTER_FRAME, hover);			this.addEventListener(Event.ENTER_FRAME, killBug);		}				public function randomRange(minNum:Number, maxNum:Number):Number {			return (Math.floor(Math.random()*(maxNum - minNum +1)) + minNum);		}				public function killBug (event: Event) { 			if(this.DC.userOneLeftHand != null){				if(this.hitTestObject(this.DC.userOneLeftHand)){				this.visible = false;				}			}					}						public function hover (event: Event) {			//Tweener.removeTweens(this);			var movement = randomRange(1, 6)			if(movement == 1) {				var newX = this.x + 40;				Tweener.addTween(this, {				 x:newX,				 time:0.4,				 transition:"easeOutCubic"				 });			}			else if(movement == 2) {				newX = this.x - 40;				Tweener.addTween(this, {				 x:newX,				 time:0.4,				 transition:"easeOutCubic"				 });			}			else if(movement == 3) {				var newY = this.y + 40;				Tweener.addTween(this, {				 y: newY,				 time:0.8,				 transition:"easeOutCubic"				 });			}			else if(movement == 4) {				newY = this.y - 40;				Tweener.addTween(this, {				 y:newY,				 time:0.8,				 transition:"easeOutCubic"				 });			}		}			}	}