package  {			import org.flintparticles.common.actions.*;		import org.flintparticles.common.counters.*;		import org.flintparticles.common.displayObjects.RadialDot;		import org.flintparticles.common.initializers.*;		import org.flintparticles.twoD.actions.*;		import org.flintparticles.twoD.emitters.Emitter2D;		import org.flintparticles.twoD.initializers.*;		import org.flintparticles.twoD.renderers.*;		import org.flintparticles.twoD.zones.*;		import flash.display.Sprite;		import flash.display.Sprite;  		import flash.filters.BlurFilter;  		import flash.filters.ColorMatrixFilter;  		import flash.geom.Rectangle;		import flash.geom.Point;		public class handCursor extends Sprite {		public function handCursor(userNumber) {						if(userNumber == 0){				var startcolour = 0x001B22E0;				var finishcolour = 0x001BB5E0;			}			else if (userNumber == 1){				startcolour = 0x00E0A51B;				finishcolour = 0x00E0C21B;			}						this.alpha = 0.9;			var smoke:Emitter2D = new Emitter2D();			smoke.counter = new Steady( 10 );						smoke.addInitializer( new Lifetime( 1, 3 ) );			smoke.addInitializer( new Velocity( new DiscSectorZone( new Point( 0, 0 ), 40, 30, -4 * Math.PI / 7, -3 * Math.PI / 7 ) ) );			smoke.addInitializer( new SharedImage( new RadialDot( 6 ) ) );			  			smoke.addAction( new Age( ) );			smoke.addAction( new Move( ) );			smoke.addAction( new LinearDrag( 0.2 ) );			smoke.addAction( new ScaleImage( 1, 9 ) );			smoke.addAction( new Fade( 0.55, 0 ) );			smoke.addAction( new RandomDrift( 15, 15 ) );			smoke.addAction( new ColorChange( startcolour, finishcolour  ) );						smoke.x = 0;			smoke.y = 0;			smoke.start( );						var renderer:BitmapRenderer = new BitmapRenderer( new Rectangle( -50, -350, 100, 400 ) );			//renderer.addEmitter( fire );			renderer.addEmitter( smoke );			addChild( renderer );		}				public function randomRange(minNum:Number, maxNum:Number):Number {			return (Math.floor(Math.random()*(maxNum - minNum +1)) + minNum);		}	}	}